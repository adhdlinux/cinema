package com.startapp;

import com.startapp.sdk.adsbase.remoteconfig.MetaDataRequest;

public interface da {
    void a(MetaDataRequest.RequestReason requestReason);

    void a(MetaDataRequest.RequestReason requestReason, boolean z2);
}
