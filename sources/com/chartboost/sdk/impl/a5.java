package com.chartboost.sdk.impl;

public interface a5 extends z4 {
    qb clearFromStorage(qb qbVar);

    qb persist(qb qbVar);

    ob refresh(ob obVar);

    ib store(ib ibVar);

    qb track(qb qbVar);
}
